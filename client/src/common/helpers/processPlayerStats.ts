import type { Player } from "src/@types";
import type { PlayerGeneralInfo } from "src/@types/player/PlayerGeneralInfo";

/**
 *
 * @param player - The player to process
 * @returns The player's general information
 */
export const processPlayerStats = (
    player: Player,
): Partial<PlayerGeneralInfo> => ({});
